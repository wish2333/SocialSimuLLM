# Minimal experiment config (v3.1.0)
# Usage: uv run socialsimullm run --config examples/minimal.yaml
# Output: runs/minimal_demo/<auto_id>/
#
# A quick 1-day (144 steps) run using all defaults.
# Suitable for first-time testing and CI validation.

simulation_steps: 144
random_seed: 42
checkpoint_interval: 10
